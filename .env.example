# Use deterministic checked-in fixtures (recommended for local development).
KNOWNGATE_MOCK=1

# For live mode, set this to the full private KnownGate API base path.
# KNOWNGATE_API_BASE=https://api.example.com/api/knowngate/v0
